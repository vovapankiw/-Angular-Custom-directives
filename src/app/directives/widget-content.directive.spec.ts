import { WidgetContentDirective } from './widget-content.directive';

describe('WidgetContentDirective', () => {
  it('should create an instance', () => {
    const directive = new WidgetContentDirective();
    expect(directive).toBeTruthy();
  });
});
