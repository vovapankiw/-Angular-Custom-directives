import { MakeUnderlineDirective } from '../make-underline.directive';

describe('MakeUnderlineDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeUnderlineDirective();
    expect(directive).toBeTruthy();
  });
});
