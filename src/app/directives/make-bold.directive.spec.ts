import { MakeBoldDirective } from './make-bold.directive';

describe('MakeBoldDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeBoldDirective();
    expect(directive).toBeTruthy();
  });
});
