import { ConditionElseDirective } from './condition-else.directive';

describe('ConditionElseDirective', () => {
  it('should create an instance', () => {
    const directive = new ConditionElseDirective();
    expect(directive).toBeTruthy();
  });
});
